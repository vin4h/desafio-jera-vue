import { services as auth } from '../modules/auth'
import { services as register } from '../modules/register'
import { services as dashboard } from '../modules/dashboard'

export default {
  auth,
  register,
  dashboard
}
